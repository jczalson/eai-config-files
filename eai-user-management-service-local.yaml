eai:
  ui:
    api-url: http://localhost:4200

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: localhost

spring:
  cloud:
    config:
      override-system-properties: false

      
listen:
  event:
    topic: false      
