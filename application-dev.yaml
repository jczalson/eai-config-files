eai:
  ui:
    api-url: http://10.1.120.2

eureka:
  client:
    serviceUrl:
      defaultZone: http://10.1.120.4:8761/eureka
  instance:
    hostname: localhost

spring:
  cloud:
    config:
      override-system-properties: false
  datasource:
    url: jdbc:h2:mem:userDB;DB_CLOSE_DELAY=-1;INIT=CREATE SCHEMA IF NOT EXISTS EAI_SCHEMA
    username: root
    password:
    driver-class-name: org.h2.Driver
  jpa:
    generate-ddl: true
    show-sql: false
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        default_schema: EAI_SCHEMA
        format_sql: true
  
      
listen:
  event:
    topic: true      
