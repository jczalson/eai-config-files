eai:
  ui:
    api-url: http://localhost:4200

eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka_service_url:http://localhost:8761/eureka}
  instance:
    hostname: localhost
    prefer-ip-address: true

spring:
  cloud:
    config:
      override-system-properties: false
  datasource:
    url: jdbc:mysql://localhost:3306/userdb;autoReconnect=true;failOverReadOnly=false;maxReconnects=10
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    generate-ddl: true
    show-sql: false
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        default_schema: EAI_SCHEMA
        format_sql: true
  
      
listen:
  event:
    topic: true      
