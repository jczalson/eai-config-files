eureka:
    instance:
    hostname: localhost
    prefer-ip-address: true
    client:
    serviceUrl:
      defaultZone: ${eureka-service-url:http://192.168.0.5:8761/eureka}

spring:
  cloud:
    config:
      override-system-properties: false
        
  
listen:
  event:
    topic: true      
